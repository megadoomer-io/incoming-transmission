# end hook — actions to run on /end BEFORE the session detaches (OPTIONAL)
#
# Run when the user types /end, before the transport tears down (closes the topic,
# removes the registry). Lines starting with '#' or blank are ignored. Empty (the
# default) = just detach, no extra actions.
#
# Example with the gstack skills (delete the '#' to use, or write your own):
#
# Invoke /track to journal the session, then /context-save to checkpoint it so a
# future session can resume this work.
