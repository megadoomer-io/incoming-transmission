# start hook — restore prior context when a session is BORN (spawned / rollover)
#
# Run by a bridge-SPAWNED session (/new or a compaction replacement) right after
# it attaches — NOT by an interactive `/telegram` attach (that session is already
# live; restoring would clobber it). The spawn prompt names the handoff file.
# Lines starting with '#' or blank are ignored.
#
# The active line below is the agnostic default. Replace it with your own
# save/restore mechanism if you have one.
Read the handoff file named in the spawn prompt to recover prior working context. If there is none, carry on fresh.
#
# If you use gstack, you may wish to restore here, e.g. /context-restore (delete the
# '#' and remove the default line above to use it).
